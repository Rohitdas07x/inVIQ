/**
 * PreviewBanner — Disabled. Preview entry is now the "Preview Demo" button on Landing.
 * Kept as a no-op component so existing imports don't break.
 */

export default function PreviewBanner() {
    return null;
}
